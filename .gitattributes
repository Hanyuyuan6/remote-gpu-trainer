# Force LF: every .sh / .template runs on a Linux remote, where a CRLF shebang or
# `do\r` silently breaks bash (references/run-remote/gotchas_universal.md U26).
* text=auto eol=lf
*.sh text eol=lf
*.template text eol=lf
*.py text eol=lf
*.md text eol=lf
