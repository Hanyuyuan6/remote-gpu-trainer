{"id": "convergence-frozen-resnet", "prompt": "Fine-tuning a ResNet50 on a rented GPU. Training runs with no errors and normal speed, but loss barely drops and val accuracy is stuck near chance. I froze the backbone with requires_grad=False and use Adam with weight_decay. How do I debug why it isn't learning?", "expect_files": ["references/training/convergence-debugging.md"], "expect_ids": ["O1", "O2", "O17", "O18", "O6"], "expect_grep": [], "must_cover": "overfit-one-batch smoke; frozen-param-still-in-optimizer; frozen-BN running-stats drift; Adam vs AdamW decoupled decay", "agentic": "PASS (1-hop, 2026-06): SKILL.md 'When training breaks' -> convergence-debugging.md O1/O2/O17/O18/O6"}
{"id": "data-worker-rng-dup", "prompt": "My image augmentations seem to repeat: different DataLoader workers produce identical random crops, and every epoch looks the same. Linux, num_workers=8, numpy-based augmentation. Real bug? Fix?", "expect_files": ["references/training/data-pipeline.md"], "expect_ids": ["DP1"], "expect_grep": ["worker_init_fn", "torch.initial_seed"], "must_cover": "numpy global RNG inherited via fork, not reseeded per worker; fix via worker_init_fn or route RNG through torch", "agentic": "PASS (1-hop, 2026-06): SKILL.md 'When training breaks' -> data-pipeline.md DP1"}
{"id": "oom-on-step-2", "prompt": "CUDA out of memory on step 2, right after the first optimizer step. Step 1 ran fine. Why does it OOM only on the second step?", "expect_files": ["references/training/oom-memory.md"], "expect_ids": ["M17"], "expect_grep": [], "must_cover": "Adam lazily allocates optimizer state (m,v) on the first step()", "agentic": "PASS (workflow w2r1t7mm9): routed to oom-memory.md ladder + step-2 entry"}
{"id": "nccl-one-rank-hang", "prompt": "Multi-GPU training hangs partway through an epoch; one rank seems stuck and the others wait forever (NCCL timeout). How do I find which rank and why?", "expect_files": ["references/training/distributed-launch.md"], "expect_ids": ["D19", "D20"], "expect_grep": [], "must_cover": "one rank diverged/OOM'd; survivors hang on the absent collective; desync-debug toolkit", "agentic": "PASS (workflow w2r1t7mm9): routed to distributed-launch.md hang toolkit"}
{"id": "diffusion-loss-low-samples-bad", "prompt": "My diffusion model's training loss is low and still decreasing, but the generated samples look bad/blurry. The loss says it's fine. What's wrong?", "expect_files": ["references/training/by-domain.md"], "expect_ids": ["DF1", "DF2"], "expect_grep": ["EMA"], "must_cover": "loss != sample quality; sampling from raw (non-EMA) weights; cross-link references/verifying/methodology.md", "agentic": "PASS (workflow w2r1t7mm9): routed to by-domain.md diffusion section"}
{"id": "nan-loss-spike-bf16", "prompt": "LLM pretraining in bf16: loss is stable then suddenly spikes to NaN. How do I find where the NaN comes from and stop the spikes?", "expect_files": ["references/training/precision-stability.md"], "expect_ids": ["P8", "P12", "P15"], "expect_grep": ["z-loss"], "must_cover": "NaN arithmetic origins + anomaly detection; LR-too-high/warmup spike; z-loss to bound logits", "agentic": "PASS (workflow w2r1t7mm9): routed to precision-stability.md"}
{"id": "resume-epoch-reset", "prompt": "I resume training from a checkpoint but the epoch/step counter restarts from 0 and the LR schedule replays warmup. What did I forget to save/restore?", "expect_files": ["references/training/checkpoint-resume.md"], "expect_ids": ["C1", "C12", "C14"], "expect_grep": [], "must_cover": "save FULL state (epoch/step/scheduler/RNG/scaler), not just weights", "agentic": "PASS (1-hop): SKILL.md -> checkpoint-resume.md"}
{"id": "throughput-gpu-starved", "prompt": "GPU utilization is low and training is slow on my rented box. I think the dataloader is starving the GPU. How do I confirm and fix it?", "expect_files": ["references/training/throughput-profiling.md"], "expect_ids": ["T1", "T4"], "expect_grep": ["num_workers"], "must_cover": "GPU-bound vs data-bound vs comms-bound triage; num_workers/prefetch knobs", "agentic": "PASS: SKILL.md -> throughput-profiling.md"}
{"id": "runpod-spot-resume-teardown", "prompt": "On RunPod my spot training keeps getting preempted. How do I make it resume instead of restarting, and how do I stop the meter most cheaply afterwards without losing checkpoints?", "expect_files": ["profiles/runpod.md"], "expect_ids": [], "expect_grep": ["terminate", "Network Volume"], "must_cover": "Network Volume is the only durable store; ~5s grace; terminate (not stop) stops billing; verify ckpt before terminate", "agentic": "PASS (workflow w2r1t7mm9): SKILL.md -> profiles/runpod.md §4/§5 -> spot-resilience.md -> checkpoint-resume.md C3"}
{"id": "vastai-teardown-billing", "prompt": "On vast.ai, what action actually stops billing, and how do I tear down without losing my checkpoints?", "expect_files": ["profiles/vastai.md"], "expect_ids": [], "expect_grep": ["destroy"], "must_cover": "destroy is the only meter-stop; stop still bills disk; copy + load-verify off-box before destroy", "agentic": "PASS (workflow w2r1t7mm9): SKILL.md -> profiles/vastai.md SS5 -> lifecycle_checklist Phase 5"}
{"id": "autodl-inode-disk-full", "prompt": "On AutoDL my torch.save fails with a disk/iostream error, but df -h shows plenty of space left. What's going on?", "expect_files": ["references/run-remote/gotchas_universal.md"], "expect_ids": [], "expect_grep": ["inode", "df -i"], "must_cover": "storage dies on inodes before bytes; monitor df -i not just df -h; millions of small files", "agentic": "PASS (workflow w2r1t7mm9): routed to the inode/disk gotcha (principle #5 / U7)"}
{"id": "china-hf-download-stall", "prompt": "Training in mainland China: a huggingface model download stalls and hangs with no error. How do I fix the download?", "expect_files": ["references/run-remote/china-network.md"], "expect_ids": [], "expect_grep": ["hf-mirror", "HF_ENDPOINT"], "must_cover": "HF_ENDPOINT=hf-mirror.com; keep hf_transfer OFF on flaky CN links; resumable-download ladder", "agentic": "PASS (workflow w2r1t7mm9): SKILL.md -> references/run-remote/china-network.md"}
{"id": "lambda-stop-vs-terminate", "prompt": "On Lambda Cloud, is there a stop action to pause billing while keeping my instance, or only terminate? How should I tear down?", "expect_files": ["profiles/lambda.md"], "expect_ids": [], "expect_grep": ["terminate"], "must_cover": "no stop state on Lambda on-demand; terminate is irreversible + wipes the instance; persistent FS is the only durable home", "agentic": "PASS (workflow w2r1t7mm9): SKILL.md -> profiles/lambda.md"}
{"id": "autodl-first-contact-15day", "prompt": "First time on AutoDL. I'll 关机 (stop) my instance between sessions to save money — is my data safe if it stays stopped for a few weeks? Anything else should I know up front?", "expect_files": ["profiles/autodl.md"], "expect_ids": [], "expect_grep": ["Surface to the user", "免密", "AD-DANGER", "mirror-research-artifacts"], "must_cover": "关机 auto-releases after 15 days -> data disk deleted (not safe to park indefinitely); for a longer pause first close canonical export/<run-id>, then pull-verify or route a durable replica to generic mirror-research-artifacts; surface conveniences (one-click SSH免密, GPU notify, panels) + danger clocks (principle #10)", "agentic": "PASS (2026-06): principle #10 first-contact surfacing -> profiles/autodl.md Surface block + AD-DANGER 15-day clock; mirror handoff updated 2026-08"}
{"id": "result-validity-report-gate", "prompt": "I just finished an ablation sweep on a rented GPU and one variant beats the baseline by 1.2% — I'm about to put it in the results table. How do I check this number is real before I report it and tear the box down?", "expect_files": ["references/verifying/methodology.md"], "expect_ids": [], "expect_grep": ["mean ± std", "normcase", "bug / effect / noise", "No fixed three/five-seed count", "Error-bar overlap is not a test of the difference"], "must_cover": "classify bug/effect/noise via control-diff before reporting; descriptive mean ± std with independent-unit design and effect uncertainty; no universal seed minimum and no inference from error-bar overlap; leakage probe on prepared artifacts; fair-comparison budget parity; re-derive from saved artifact before reporting", "agentic": "PASS (2-hop, 2026-06): SKILL.md VERIFY section -> references/verifying/methodology.md sections 1/4/5/9/14", "historical_must_cover": "classify bug/effect/noise via control-diff before reporting; >=3-seed mean+-std (win inside error bars = noise); leakage probe on prepared artifacts (normcase/same-bytes hash); fair-comparison budget parity; re-derive from saved artifact before reporting", "expectation_amendment": "2026-09-06: fixed seed sufficiency and overlap=noise retired after independent obligation review and NIST paired-inference/power checks. Original prompt/keywords and historical expectation retained; June agentic PASS is historical, not a rerun of this amendment."}
{"id": "loss-spike-data-vs-optimizer-state", "prompt": "Loss spiked twice mid-pretraining then recovered. Colleagues say 'a bad data batch' but I'm not sure — how do I tell a dirty-data spike from an optimizer-state one, and what is the recovery?", "expect_files": ["references/training/precision-stability.md"], "expect_ids": ["P11", "P14"], "expect_grep": ["replay", "state interaction", "roll back"], "must_cover": "replay-from-earlier-checkpoint diagnostic (PaLM: spikes did not reproduce => state, not data); rollback ~100 steps + skip batches; P11 reproducible-step vs step-varying fork", "agentic": "PASS (blind old-vs-new eval, 2026-07): 1-hop SKILL.md -> precision-stability.md; replay diagnostic reached in P14, single-sourced; the same eval's discoverability finding produced the P11->P14 pointer"}
{"id": "determinism-declared-not-achieved", "prompt": "My runs set torch.manual_seed(42) yet two 'identical' runs give different curves, and augmentations look identical across DataLoader workers. Is my setup actually deterministic? How do I audit it, and what is the complete knob set?", "expect_files": ["references/verifying/methodology.md", "references/training/precision-stability.md", "references/training/data-pipeline.md"], "expect_ids": ["P19", "DP1"], "expect_grep": ["declared, not achieved", "worker_init_fn", "CUBLAS_WORKSPACE_CONFIG"], "must_cover": "audit stance: manual_seed alone = declared-not-achieved (grep the entrypoint for worker_init_fn / use_deterministic_algorithms); the full knob set stays single-sourced in P19 + DP1", "agentic": "PASS (blind old-vs-new eval, 2026-07): methodology sec 13 -> P19 + DP1 cross-links followed; complete knob set reached; cross-link architecture judged the intended path, no dead ends"}
{"id": "disk-budget-gate-not-log", "prompt": "My queue script prints df before each run, but the disk still filled mid-queue and six downstream runs crashed in a row. How should the disk-budget check actually work?", "expect_files": ["references/run-remote/gotchas_universal.md"], "expect_ids": ["U6"], "expect_grep": ["DISK_GATE_ABORT", "GATE, not a log line"], "must_cover": "a printed df is not a gate: compute free_gb and abort (exit 3) below the floor BEFORE every invocation; prune between batched runs", "agentic": "not yet run (structural-only guard, added 2026-07 alongside the disk-gate content)"}
{"id": "teardown-exact-manifest-gate", "prompt": "I already have an old 3 GB checkpoint folder locally. Can I load that old checkpoint and release the GPU without reading the new canonical remote back?", "expect_files": ["SKILL.md", "references/run-remote/lifecycle_checklist.md"], "expect_ids": [], "expect_grep": ["independent temporary consumer", "full prediction population", "SHA-256", "not a universal custody requirement"], "must_cover": "never use size or an old loadable artifact as completeness evidence; restore the exact canonical remote to an independent temporary consumer, match roster/bytes/hash, safe-load, fully recompute metrics, and require user approval; the consumer may be remote and a resident Mac checkpoint is optional", "agentic": "structural safety regression updated 2026-09"}
{"id": "durable-sync-failure-propagates", "prompt": "Training exited 0 but copying best.pth to durable storage failed. Should the queue still mark the cell successful?", "expect_files": ["scripts/run_one.sh.template"], "expect_ids": [], "expect_grep": ["SYNC_EXIT=2", "exit \"$SYNC_EXIT\""], "must_cover": "durable sync failure is independently non-zero and cannot be hidden by training exit 0", "agentic": "structural safety regression added 2026-07"}
{"id": "zero-aggregation-must-fail", "prompt": "The aggregation directory had no completed cells, but the script printed 0 failed. Is that a valid success before teardown?", "expect_files": ["scripts/aggregate_to_fs.sh"], "expect_ids": [], "expect_grep": ["zero complete ablations", "EXPECTED_ROSTER_FILE"], "must_cover": "zero aggregation fails; final manifest requires an explicit expected roster and run id", "agentic": "structural safety regression added 2026-07"}
{"id": "multinode-producer-is-not-accepted", "prompt": "Four rented nodes write to one shared result disk. A complete.json appeared and tmux exited. Can I count the cell done and sync the report?", "expect_files": ["references/run-remote/production-matrix-acceptance.md"], "expect_ids": [], "expect_grep": ["producer complete", "independently accepted", "scope blocker", "weights_only=True"], "must_cover": "producer marker is not independent acceptance; distinguish blockers, failed attempts, live claims, and accepted artifacts", "agentic": "structural safety regression added 2026-07"}
{"id": "partial-scp-needs-index-reconciliation", "prompt": "SCP returned after a retry and most evidence files are present. Is that enough to release the instances?", "expect_files": ["references/run-remote/production-matrix-acceptance.md"], "expect_ids": [], "expect_grep": ["evidence indexes", "hash-identical", "strict host-key"], "must_cover": "rebuild local/remote indexes after interrupted transfer; keep strict host-key checks; only hash-identical copies count", "agentic": "structural safety regression added 2026-07"}
{"id": "tmux-wrapper-pkill-hazard", "prompt": "A queue was launched through run_queue.sh in tmux. Can I use pkill -f run_queue.sh to stop just that job?", "expect_files": ["references/run-local/launch.md"], "expect_ids": [], "expect_grep": ["Process-identity trap", "tmux kill-session", "worker PID/argv"], "must_cover": "wrapper-name pkill can match the tmux server and kill unrelated sessions; target the exact session or verified worker PID", "agentic": "memory distillation regression 2026-08"}
{"id": "incomplete-checkpoint-quarantine", "prompt": "A retry found checkpoints without a completion sentinel. Delete that old directory before rerunning?", "expect_files": ["references/verifying/smoke-hidden-failures.md", "references/verifying/methodology.md"], "expect_ids": [], "expect_grep": ["quarantine", "explicit authorization", "user artifacts"], "must_cover": "checkpoint presence is not completion; isolate and preserve suspect artifacts, then ask before deletion", "agentic": "memory distillation regression 2026-08"}
{"id": "authorization-layering-no-reask", "prompt": "I already granted Full Access and authorized a non-overwriting remote pull. A verifier schema bug now needs verifier-v3 and a new immutable contract, but the checkpoint, protocol and cost are unchanged. Must the agent ask me again?", "expect_files": ["SKILL.md", "references/run-remote/production-matrix-acceptance.md"], "expect_ids": [], "expect_grep": ["Execution permission is not task authority", "Authorization continuity", "do not re-ask"], "must_cover": "sandbox permission, operational authority and scientific promotion are separate; same-scope non-overwriting control-plane repairs continue under the existing authorization, while new cost/science/destructive/publication scope requires a new decision", "agentic": "behavioral regression distilled from a repeated remote evidence-closure approval loop 2026-08"}
{"id": "real-schema-fixture-drift", "prompt": "The pulled checkpoint roster and hashes all match, but verifier-v2 rejects a real completion JSON because its synthetic positive fixture invented a top-level latest_epoch field. Should I monkeypatch the verifier or reread the 5 GB bundle until it passes?", "expect_files": ["SKILL.md", "references/run-remote/production-matrix-acceptance.md"], "expect_ids": [], "expect_grep": ["synthetic fixture", "real-shape positive fixture", "Control plane", "Data plane"], "must_cover": "preserve the frozen bad verifier, mint a new immutable verifier/contract against the real producer schema, prove test liveness, clear the small control plane first, and read the large payload once per trust boundary without bypassing the gate", "agentic": "behavioral regression distilled from repeated boolean/object and missing-field fixture drift 2026-08"}
{"id": "shared-fs-node-local-split-pull", "prompt": "A verified bundle lives on a slow shared FUSE mount and contains two multi-GB checkpoints plus hundreds of tiny evidence files. How can I speed up the Windows pull without weakening provenance?", "expect_files": ["references/run-remote/ssh_transport.md"], "expect_ids": [], "expect_grep": ["node-local transport cache", "deterministic tar", "empty directories", "separate resumable files"], "must_cover": "freeze the durable roster first, verify a unique node-local cache, tar only the explicit small-file roster, transfer large checkpoints separately and resumably, preserve empty-directory semantics, then verify the local staging; cache is not a durable replica", "agentic": "behavioral regression distilled from shared-AutoFS evidence pull 2026-08"}
{"id": "autodl-canonical-export-and-mirror-boundary", "prompt": "My AutoDL data disk mixes caches, rolling checkpoints, logs, software results, and real-capture hardware outputs. What exact capsules should I close, and can I sync the whole active project?", "expect_files": ["SKILL.md", "profiles/autodl.md", "references/run-remote/artifact-layout.md", "references/run-remote/lifecycle_checklist.md", "references/delivering/legacy-publication-guidance.md", "scripts/manifest_scaffold.py", "scripts/repro.sh.template"], "expect_ids": [], "expect_grep": ["compute/control layer", "/root/autodl-tmp/<project>/{cache,active,export/.partial,export/<run-id>,quarantine}", "latest.pth", "best.pth", "last.pth", "run.json", "config.yaml", "train.csv", "test/<test-id>", "metrics.json", "results.parquet", "vis/<condition-id>/<task-native-role>/<sample-id>.png", "Visualization PNGs are required", "complete Cartesian coverage", "no top-level `config/`", "no dataset bytes", "_trust/selections/<selection-id>.json", "second selection manifest", "legacy visualization index", "contains no full-file roster", "external frozen manifest", "A software `export/<run-id>` must never contain", "export/hardware/<hardware-run-id>", "never copies `best.pth`", "declared hardware test", "legacy/non-canonical publication synthesis guidance", "Reject `results/<exp-id>/runs/<run-id>`", "figure.json", "fully isomorphic", "atomic rename", "quarantine/", "Never sync `active/` as a whole", "must not add `COMPLETE.json`", "mirror-research-artifacts", "min(100, N_test)", "selection_id", "never choose a performance-ranked top 100", "Create an empty canonical software-run directory scaffold", "Canonical, project-specific reproduction template"], "expect_each_files": ["references/delivering/legacy-publication-guidance.md"], "expect_each_grep": ["legacy/non-canonical publication synthesis guidance"], "reject_each_files": ["scripts/manifest_scaffold.py", "scripts/repro.sh.template"], "reject_each_grep": ["results/<exp-id>/runs", "selected/checkpoints"], "must_cover": "exact software capsule remains run.json/config.yaml/train.csv/best.pth/optional last.pth/test outputs with mandatory conditions x roles x K vis; software export never carries hardware results; hardware closes independently under export/hardware/<hardware-run-id> or hygiene, binds capture/decode/model-run and best hash, copies no weights, and has mandatory vis; run.json has scientific bindings only and no exact file roster/bytes/hash inventory; generic mirror owns the external frozen manifest and live exact-roster validation; delivering is legacy/non-canonical synthesis only, rejects results/<exp>/runs, checkpoints dir, qualitative tree, selected symlink and nested README/output/provenance workshop; canonical executable helpers never recreate those legacy paths; never whole-sync active", "agentic": "structural regression updated 2026-08; behavioral re-run pending"}
{"id": "autodl-hardware-no-machine-readable-gt", "prompt": "A real-hardware detection export has finite predictions but no machine-readable task ground truth. Should I add placeholder GT/AP so the canonical hardware capsule closes?", "expect_files": ["SKILL.md", "references/run-remote/artifact-layout.md", "references/run-remote/lifecycle_checklist.md", "references/delivering/legacy-publication-guidance.md"], "expect_ids": [], "expect_grep": ["ground_truth_status=unavailable_no_machine_readable_gt", "metric_applicability=not_applicable", "finite-forward rows", "prediction/overlay", "never invents ground truth", "accuracy, AP, IoU"], "must_cover": "declare no machine-readable GT explicitly, preserve finite-forward rows and task-applicable atomic visuals, forbid fake GT-derived metrics, and route canonical validation to research-artifact-hygiene", "agentic": "compatibility regression for honest real-hardware closeout 2026-08"}
{"id": "ssh-loss-is-not-remote-state", "prompt": "后台训练期间 SSH 先 connection refused，重试后又出现 host-key mismatch。Agent 能否据此断言训练、实例或计费已停止并直接报告 blocked？", "expect_files": ["SKILL.md", "profiles/autodl.md", "references/run-remote/monitoring_patterns.md", "references/run-remote/ssh_transport.md"], "expect_ids": [], "expect_grep": ["After one failed package and one", "successor, stop version churn", "After restart, compaction, or transport loss, re-probe", "don't read \"can't connect\" as \"the box died.\"", "never from connection refusals alone", "loss does not change remote state", "A connection error alone does not establish run", "instance, or billing state.", "provider console or another authenticated channel", "Never delete the old key and blindly re-accept"], "must_cover": "use the maintained direct path and at most one bounded successor before a transport blocker; transport failure alone proves neither training nor instance nor billing state; after reconnect, re-probe process/session/artifact truth; close a host-key mismatch only through an authenticated fingerprint channel", "agentic": "cross-platform relocation of three retired Windows transport regressions; structural and fail-capable, not a live network test"}
{"id": "control-plane-economy-one-action", "prompt": "A bounded remote objective is already authorized. The worker needs a read-only diagnosis, a same-scope repair to an unexecuted gate, one non-overwriting launch, and then a multi-hour wait. How many approval and monitoring layers are appropriate?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Read-only diagnosis, offline preparation", "one executable action decision", "first side effect", "generic proof framework", "durable event-driven monitoring", "without waking a model", "material delta"], "must_cover": "read-only diagnosis and same-scope repair continue under existing authority; the remote mutation uses one executable decision, receipt, and postcheck; no nested proof framework or unrelated scanner; a durable watcher owns frequent heartbeats and the model wakes only on material events or an agreed sparse cadence", "agentic": "behavioral regression distilled from a live project and a live project closeout control-plane churn 2026-08-12"}
{"id": "standard-action-successor-budget", "prompt": "A detached launch failed during package formatting, its successor failed in the real remote shell before training started, and the worker proposes V3 under a renamed action id. Is that allowed?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["one initial package plus one successor", "A third version is forbidden", "renaming", "maintained primitive", "minimal operator-visible command"], "must_cover": "all pre-science failures count against one action budget; renaming does not reset it; preserve immutable failure receipts by pointer/hash; do not build V3; repair/use the maintained primitive, use a minimal visible command, or report one blocker", "agentic": "behavioral regression from a live project sampled100 V1-V11 gate churn 2026-08-12"}
{"id": "real-consumer-shell-dry-run", "prompt": "The generated detached-launch JSON passes schema tests, but the actual remote command creates a socket directory whose parent may not exist. What must pass before the gate is frozen?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["producer -> serializer", "consumer/parser -> runner -> target shell/OS", "directory ancestry", "Mocks may suppress", "must not replace the real parser or runner"], "must_cover": "exercise the exact producer through actual consumer/parser and runner in the target shell/OS with real path, quoting, directory ancestry, ownership/mode, timestamp and exit propagation; mock only external side effects; catch the missing parent before remote execution", "agentic": "behavioral regression from a live project V10 detached launcher failure 2026-08-12"}
{"id": "bulk-payload-residency-and-fault-domain", "prompt": "The user wants local indexes and paper evidence only, while 386 GB of payload lives remotely. Can I download all of it and prove restore by copying C: to D: when both are the same SSD?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Preflight topology before moving bulk data", "required residency", "local claim plane", "remote-to-remote", "one physical device", "independent failure domain"], "must_cover": "record topology before a >1 GiB or >15 minute transfer; keep local claim-plane artifacts small; validate or mirror payload remote-to-remote absent local-residency authority; same physical disk is not independent restore evidence", "agentic": "behavioral regression from a live project HF full-readback priority inversion 2026-08-12"}
{"id": "thin-receipt-no-embedded-control-plane", "prompt": "A routine launch receipt is 470 KiB and embeds the controller, remote script, full roster, and captured output. Is this stronger evidence?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Keep receipts thin", "64 KiB receipt", "pointers/hashes", "Do not embed controllers", "Immutability applies to the receipt"], "must_cover": "receipt contains decision, identities, result and content-addressed pointers; logs/manifests stay external; embedded scripts and outputs increase churn without adding trust; apply the default 64 KiB ceiling", "agentic": "behavioral regression from a live project V10 receipt amplification 2026-08-12"}
{"id": "autodl-release-guard-retires-after-pull", "prompt": "An old worker charter says never release this AutoDL instance because data was not local. The exact roster is now locally PULL_VERIFIED and I explicitly authorize release to end storage billing. Must the old no-release rule still win?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md", "profiles/autodl.md"], "expect_ids": [], "expect_grep": ["Retire provisional prohibitions", "state guard, not a permanent `never release`", "current user authorizes release", "execute the shortest", "supported release path", "deletes `/`", "data disk irreversibly"], "must_cover": "verify exact local custody and current user authority; retire the provisional no-release guard; bind the exact instance; use one provider release and disappearance/billing postcheck; do not delete local evidence or confuse release with arbitrary file deletion", "agentic": "behavioral regression from a live project A release authority supersession 2026-08-12"}
{"id": "autodl-release-ready-but-shutdown-only", "prompt": "I want A/B data pulled so losing the instances later would not matter, but for now shut them down and do not release. What action is authorized?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md", "profiles/autodl.md"], "expect_ids": [], "expect_grep": ["Do not confuse release-readiness with release authority", "custody acceptance criterion, not permission to destroy", "shut down, do not release", "normal shutdown preflight", "preserve the provider disks"], "must_cover": "finish exact pull and local semantic acceptance; perform one verified-idle shutdown and offline probes; preserve disks; do not execute or prepare release merely because release-readiness is the quality target", "agentic": "behavioral regression from a live project user clarification 2026-08-12"}
{"id": "plain-language-material-action-preflight", "prompt": "A remote closeout is about to move 386 GB for several hours to a local disk. The authority already exists. What user-visible report is required before starting?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["zero-surprise action summary", "plain-language message", "expected data/time/cost", "destination, reversibility, and stop", "does not reopen already matching authority"], "must_cover": "before the first side effect state result, action, target, expected size/time/cost, destination, reversibility and stop condition in plain language; do not dump packet names or ask again when scope is already authorized; report only material deltas afterward", "agentic": "behavioral regression from surprise 386 GB local readback 2026-08-12"}
{"id": "autodl-no-shebang-shutdown-wrapper", "prompt": "Verified-idle passed, but Python shell=False gets Exec format error because /usr/bin/shutdown is a 205-byte AutoDL ASCII wrapper without a shebang. Must shutdown remain blocked?", "expect_files": ["profiles/autodl.md"], "expect_ids": [], "expect_grep": ["Container shutdown wrapper", "handles `ENOEXEC`", "Exec format error", "complete bounded body", "recognized provider", "/root/.local/share/Trash", "unambiguous stop operation", "stable `supervisord` PID", "Do not require ELF/shebang"], "must_cover": "do not treat the missing ELF/shebang as terminal by itself; resolve and inspect the entire small root-owned non-writable wrapper, hash and semantically accept only known bounded operations; report its Trash cleanup consequence; if all effects are authorized invoke it through /bin/sh, otherwise isolate only the fully proven stop operation with exact stable target identity or use provider shutdown; never guess from a partial body or delete research evidence", "agentic": "behavioral regression from a live project A verified-idle shutdown ENOEXEC 2026-08-12"}
{"id": "capacity-drift-is-not-capacity-failure", "prompt": "A sampled evaluation has sufficient disk space and the same filesystem device throughout preflight, but available_bytes changes slightly while logs and temporary files are created. Should the runner abort because the two values are not identical?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Capacity is a threshold, not an equality invariant", "required bytes and safety margin", "space drift from logs", "Never require two", "capacity falling below the frozen floor"], "must_cover": "bind device id, non-overwrite target, required bytes and safety margin; allow normal capacity drift while every sample stays above the floor; fail on device change, conflicts, incomplete query or below-floor capacity, not exact byte inequality; avoid a self-invalidating launch gate", "agentic": "behavioral regression from a live project B sampled100 capacity changed during preflight 2026-08-12"}
{"id": "shutdown-preflight-excludes-own-ssh-chain", "prompt": "A verified-idle shutdown preflight runs over SSH and sees its own `sshd: accepted` session. The privilege-separation ancestor denies /proc/pid/exe and cwd while stat/status/cmdline are readable. Is this an active-user or incomplete-query blocker?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["must not reject its own control connection", "PID/PPID/starttime/session/cmdline", "never use grep/substrings", "privilege-separation ancestor", "not global query incompleteness", "any extra SSH session"], "must_cover": "construct and bind the exact executor ancestor chain, exclude only that chain from other-session checks, accept expected exe/cwd denial only for the bound privilege-separation ancestor when alternative proc fields close identity, and continue to fail on extra sessions, unbound permission errors or ancestry gaps", "agentic": "behavioral regression from a live project A shutdown self-session rejection 2026-08-12"}
{"id": "scientific-source-successor-is-not-control-vn", "prompt": "A one-line capacity bug lives inside a frozen content-addressed 102-file runner. The runner verifies source bytes and plan identity before use. The action package already exhausted its successor budget. May the authorized experiment create a fixed source identity?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Do not apply the control-package budget to scientific identities", "protocol-preserving bug", "never patch the frozen source or bypass", "scientific source successor", "fresh run/output identity", "not a renamed action-gate Vn"], "must_cover": "keep frozen source immutable and identity checks live; create one minimal nonoverwriting protocol-preserving source successor and exact plan binding under existing outcome authority; test the changed behavior via the real consumer and use a fresh scientific output; do not count it as control Vn or add wrappers/review frameworks", "agentic": "behavioral regression from a live project B capacity guard source-identity conflict 2026-08-12"}
{"id": "provider-storage-symlink-resolves-to-safe-target", "prompt": "AutoDL exposes /root/autodl-fs as a root-owned symlink with lstat mode 0777 and 23 GB free behind it. Is the symlink mode alone enough to reject it as an output filesystem?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Resolve provider storage aliases before judging them", "conventional mode `0777` says nothing", "alias owner and link text", "mount id", "fresh `O_EXCL` child", "bound canonical path"], "must_cover": "do not treat a provider alias symlink or its conventional lstat mode as proof of unsafe storage; bind alias identity, resolve a stable ancestry, validate the canonical target and mount identity, capacity and writer state, use a fresh exclusive child on the canonical path, and fail closed if resolution is mutable or untrusted", "agentic": "behavioral regression from a live project B alternate filesystem rejection 2026-08-12"}
{"id": "screen-absence-probe-must-not-create-screendir", "prompt": "A preflight wants to prove a Screen session is absent. SCREENDIR itself does not exist, but running screen -Q would create or touch it. Is that still a read-only probe?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Prove that read-only probes are observational", "invoking Screen with", "missing namespace already proves absence", "metadata equality before and after"], "must_cover": "treat absence of the reviewed namespace as sufficient; do not invoke a utility known to materialize state; behaviorally test the probe on an absent path and require no entry, byte or metadata change before allowing it in a pre-side-effect gate", "agentic": "behavioral regression from a live project B Screen absent probe creating SCREENDIR 2026-08-12"}
{"id": "postcheck-must-bind-actual-runtime-namespace", "prompt": "A remote launch was dispatched but local stdout capture failed. A postcheck looks only at an older planned target and session and reports NOT_RUNNING, while the actual argv uses a different target/session and LAUNCH exists. May the action be retried?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Postcheck the identity that actually ran", "current process argv", "never infer remote failure or retry", "local capture defect does not erase remote truth", "wrong session/path cannot support `NOT_RUNNING`"], "must_cover": "do not retry after ambiguous dispatch; derive exact runtime target/session/socket from the consumed action plus live process argv, inspect those namespaces and markers, reconcile local capture failure separately, and reject NOT_RUNNING based on stale constants", "agentic": "behavioral regression from a live project B false negative postcheck after lost host capture 2026-08-12"}
{"id": "artifact-cardinality-must-match-producer-roles", "prompt": "A sampled evaluation contract requires exactly 5000 records and 5000 PNGs with an inode budget of 5000. Detection records publish an overlay and, when reconstruction exists, a second convenience reconstruction PNG. The 5001st publish fails. Should we raise the inode budget?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md"], "expect_ids": [], "expect_grep": ["Reconcile artifact cardinality at the producer boundary", "actual multiplicity per accepted record", "exactly `N` task-native PNGs", "extra convenience artifact", "Do not raise a budget", "`N`/`N+1` boundary"], "must_cover": "do not mask the producer/contract mismatch by raising the budget; bind expected counts to declared artifact roles and actual multiplicity; if the contract is one task-native PNG per record, preserve one task-native image and numeric auxiliary measurements while removing or separately declaring extra convenience assets; test one-record and N/N+1 boundaries through the real producer", "agentic": "behavioral regression from a live project B sampled100 v4 5001st PNG failure 2026-08-12"}
{"id": "storage-pressure-active-recovery-ladder", "prompt": "A rented node is at 96.64% system disk and 94.71% data disk, and the next run cannot meet its frozen space floor. Should the agent keep blocking, delete broadly, or shrink the experiment?", "expect_files": ["SKILL.md", "references/run-remote/control-economy.md", "references/run-remote/gotchas_universal.md", "references/run-remote/lifecycle_checklist.md"], "expect_ids": ["U7"], "expect_grep": ["storage-pressure recovery ladder", ">=90%", "required + margin", "proven-regenerable", "fresh private prefix", "independent restore", "semantic load", "exact shortfall", "recommended expansion", "restart requirement"], "must_cover": "treat percentage as early warning and absolute required-plus-margin as the gate; stop adding new writers; inventory the resolved mount and live writers; protect active, unknown and research-bearing paths; proactively reclaim only exact proven-regenerable task-local scratch with a receipt; mirror valuable portable artifacts to a fresh private HF/durable prefix and require exact roster/hash plus independent restore semantic verification before cleanup eligibility; if still short, report exact expansion and restart needs rather than minting gates or shrinking science", "agentic": "behavioral regression from a live project AutoDL 96.64/94.71 percent storage pressure 2026-08-12"}
{"id": "versioned-top100-selection-export", "prompt": "I approved a main-model PSNR Top 100 for qualitative MNIST examples. May AutoDL reuse the old fixed-model-blind rule, rank a rounded 100-row file, or write separate task rosters?", "expect_files": ["profiles/autodl.md", "references/run-remote/artifact-layout.md", "references/run-remote/lifecycle_checklist.md", "references/delivering/legacy-publication-guidance.md"], "expect_ids": [], "expect_grep": ["schema 2", "fixed_model_blind", "Schema 3", "MNIST test", "K=512 clean float32", "complete unrounded", "sample ID ascending", "reconstruction", "segmentation", "detection", "safe project-relative", "full-test metrics", "No-GT", "qualitative examples only", "fairness"], "must_cover": "preserve schema2 exactly; schema3 binds exact main model/config/checkpoint and complete source population, independently sorts unrounded reconstruction PSNR descending with canonical sample-ID ties and K=min(100,N), reuses one ordered roster across methods conditions and all three tasks, preserves full-test population, blocks general/fair claims, and uses a distinct explicit non-PSNR roster for no-GT", "agentic": "selection protocol regression added for approved schema3 scientific policy 2026-09"}
